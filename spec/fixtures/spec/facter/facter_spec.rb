require_relative '../../../spec_helper'

describe 'I am a standard RSpec test.' do
  it 'Outputs hello world for tasks_spec to test against with a stdout matcher chain.' do
    expect(true).to eq(true)
  end
end
